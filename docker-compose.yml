services:
    app:
        image: node:20
        command: "npm run dev -- --host 0.0.0.0"
        volumes:
            - ./app:/app
            - .env:/app/.env
        working_dir: /app
        ports:
            - 5173:5173
        environment:
            - HOST=0.0.0.0